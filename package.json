{
  "name": "unifast-monorepo",
  "private": true,
  "scripts": {
    "lint": "oxlint",
    "fmt": "oxlint --fix",
    "test": "vitest run",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0",
    "oxlint": "^0.15.0"
  }
}
