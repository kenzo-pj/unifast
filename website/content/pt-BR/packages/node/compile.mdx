---
title: "compile()"
description: "A função primária de compilação. Transforma Markdown ou MDX em HTML ou outros formatos de saída usando o compilador Rust nativo."
---

```ts
import { compile } from "@unifast/node";
```

## Assinatura

```ts
function compile(input: string, options?: CompileOptions): CompileResult
```

## Parâmetros

### input

| Propriedade | Tipo | Padrão | Descrição |
|----------|------|---------|-------------|
| `input` | `string` | — | String de fonte Markdown ou MDX |

### options?

| Propriedade | Tipo | Padrão | Descrição |
|----------|------|---------|-------------|
| `options?` | `CompileOptions` | — | Configuração de compilação incluindo plugins |

## Retorna

| Propriedade | Tipo | Descrição |
|----------|------|-------------|
| `output` | `string \| object` | Saída compilada. String HTML por padrão; string JSON para HAST/MDAST. |
| `frontmatter` | `Record<string, unknown>` | Metadados de frontmatter parseados (`{}` vazio se nenhum) |
| `diagnostics` | `Diagnostic[]` | Array de `{ level, message, start?, end? }` |
| `stats` | `{ parseMs, transformMs, emitMs }` | Detalhamento de tempo (ms) |
| `toc` | `TocEntry[]` | Table of contents extraída (`[]` vazio se TOC desabilitado) |

## Uso

```ts
import { compile, gfm, frontmatter, toc, sanitize } from "@unifast/node";

const md = `---
title: My Document
---

# Hello, **world**!

| Column A | Column B |
|----------|----------|
| Cell 1   | Cell 2   |
`;

const result = compile(md, {
  inputKind: "md",       // "md" | "mdx"
  outputKind: "html",    // "html" | "hast" | "mdast" | "mdxJs"
  plugins: [gfm(), frontmatter(), toc({ maxDepth: 3 }), sanitize()],
});

console.log(result.output);
// <h1 id="hello-world">Hello, <strong>world</strong>!</h1>
// <table>...</table>

console.log(result.frontmatter);
// { title: "My Document" }

console.log(result.toc);
// [{ depth: 1, text: "Hello, world!", slug: "hello-world" }]

console.log(result.stats);
// { parseMs: 0.12, transformMs: 0.08, emitMs: 0.05 }
```

## Exemplos

### Markdown básico para HTML

```ts
import { compile } from "@unifast/node";

const result = compile("# Hello, **world**!");

console.log(result.output);
// <h1 id="hello-world">Hello, <strong>world</strong>!</h1>
```

### Entrada MDX

```ts
import { compile } from "@unifast/node";

const mdx = `
import { Alert } from "./components";

# Hello

<Alert type="info">This is MDX!</Alert>
`;

const result = compile(mdx, { inputKind: "mdx" });
```

### Saída HAST

```ts
import { compile } from "@unifast/node";

const result = compile("# Hello", { outputKind: "hast" });
const hast = JSON.parse(result.output as string);

console.log(hast);
// { type: "root", children: [{ type: "element", tagName: "h1", ... }] }
```

### Saída MDAST

```ts
import { compile } from "@unifast/node";

const result = compile("# Hello", { outputKind: "mdast" });
const mdast = JSON.parse(result.output as string);

console.log(mdast);
// { type: "root", children: [{ type: "heading", depth: 1, ... }] }
```

### Saída MDX JS

```ts
import { compile } from "@unifast/node";

const result = compile(mdxSource, {
  inputKind: "mdx",
  outputKind: "mdxJs",
});

console.log(result.output);
// JavaScript module string with MDX compiled to JSX
```
