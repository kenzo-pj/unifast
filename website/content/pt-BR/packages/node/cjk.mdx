---
title: "cjk()"
description: "Processamento de texto compatível com CJK que remove newlines indesejadas entre caracteres CJK."
---

import { Example } from "~/components/Example"
import example from "./cjk-example.md?example"

```ts
import { cjk } from "@unifast/node";
```

## Assinatura

```ts
function cjk(): UnifastPlugin
```

## Parâmetros

Nenhum.

## Uso

```ts
import { compile, cjk } from "@unifast/node";

const md = `
今日は天気が
とても良いです。
`;

const result = compile(md, { plugins: [cjk()] });
console.log(result.output);
// <p>今日は天気がとても良いです。</p>
```

## Exemplos

### Remoção de newlines em CJK

<Example data={example} />
