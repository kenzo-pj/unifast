---
title: "Início Rápido"
description: "Instale o unifast e compile seu primeiro documento Markdown em menos de um minuto."
---

import { PackageInstall } from "~/components/PackageInstall"

Instale o unifast e compile seu primeiro documento Markdown em menos de um minuto.

### Instalação

<PackageInstall package="@unifast/node" />

### Uso Básico

```ts
import { compile } from "@unifast/node";

const result = compile("# Hello, unifast!\n\nThis is **Markdown**.");

console.log(result.html);
// <h1>Hello, unifast!</h1>
// <p>This is <strong>Markdown</strong>.</p>
```

É isso. Um import, uma chamada de função, saída em HTML.

### Adicionando Plugins

Plugins estendem o compilador com funcionalidades adicionais. A maioria dos plugins está incluída em `@unifast/node` e configura os passes integrados.

```ts
import { compile, frontmatter, gfm } from "@unifast/node";

const source = `---
title: My Post
date: 2025-01-15
---

# My Post

A table:

| Feature | Status |
|---------|--------|
| GFM     | Yes    |

- [x] Task complete
- [ ] Task pending
`;

const result = compile(source, {
  plugins: [frontmatter(), gfm()],
});

console.log(result.frontmatter);
// { title: "My Post", date: "2025-01-15" }

console.log(result.html);
// Rendered HTML with GFM table and task list
```

### Adicionando Syntax Highlighting

```ts
import { compile, syntect } from "@unifast/node";

const result = compile(
  '```js\nconsole.log("highlighted");\n```',
  { plugins: [syntect()] }
);
// Code block with syntax highlighting classes
```

### Próximos Passos

- [Conceitos Principais](/docs/introduction/key-concepts) - Entenda a pipeline de compilação e a arquitetura.
- [Syntax Highlighting](/docs/guides/syntax-highlighting) - Configure o highlighting de code blocks em detalhes.
- [compile()](/docs/packages/node/compile) - Referência completa da API da função compile.
