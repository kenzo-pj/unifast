---
title: "autolinkHeadings()"
description: "Añade automáticamente enlaces de ancla a los encabezados."
---

import { Example } from "~/components/Example"
import example from "./autolink-headings-example.md?example"

```ts
import { autolinkHeadings } from "@unifast/node";
```

## Firma

```ts
function autolinkHeadings(options?: AutolinkHeadingsOptions): UnifastPlugin
```

## Parámetros

### options?

Configuración del comportamiento de los enlaces de encabezado

| Propiedad | Tipo | Por defecto | Descripción |
|----------|------|---------|-------------|
| `behavior` | `"prepend" \| "append" \| "wrap"` | `"prepend"` | Dónde colocar el enlace de ancla en relación con el contenido del encabezado |

## Uso

```ts
import { compile, autolinkHeadings } from "@unifast/node";

const md = `# Introduction

## Getting Started

### Installation`;

const result = compile(md, {
  plugins: [
    autolinkHeadings({
      behavior: "prepend", // "prepend" | "append" | "wrap"
    }),
  ],
});
```

## Ejemplos

### Encabezados con enlaces de ancla

<Example data={example} />
