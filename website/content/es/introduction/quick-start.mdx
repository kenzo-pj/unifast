---
title: "Inicio rápido"
description: "Instala unifast y compila tu primer documento Markdown en menos de un minuto."
---

import { PackageInstall } from "~/components/PackageInstall"

Instala unifast y compila tu primer documento Markdown en menos de un minuto.

### Instalación

<PackageInstall package="@unifast/node" />

### Uso básico

```ts
import { compile } from "@unifast/node";

const result = compile("# Hello, unifast!\n\nThis is **Markdown**.");

console.log(result.html);
// <h1>Hello, unifast!</h1>
// <p>This is <strong>Markdown</strong>.</p>
```

Eso es todo. Un import, una llamada a una función, salida HTML.

### Añadir plugins

Los plugins extienden el compilador con funcionalidades adicionales. La mayoría están incluidos en `@unifast/node` y configuran pases integrados.

```ts
import { compile, frontmatter, gfm } from "@unifast/node";

const source = `---
title: My Post
date: 2025-01-15
---

# My Post

A table:

| Feature | Status |
|---------|--------|
| GFM     | Yes    |

- [x] Task complete
- [ ] Task pending
`;

const result = compile(source, {
  plugins: [frontmatter(), gfm()],
});

console.log(result.frontmatter);
// { title: "My Post", date: "2025-01-15" }

console.log(result.html);
// Rendered HTML with GFM table and task list
```

### Añadir resaltado de sintaxis

```ts
import { compile, syntect } from "@unifast/node";

const result = compile(
  '```js\nconsole.log("highlighted");\n```',
  { plugins: [syntect()] }
);
// Code block with syntax highlighting classes
```

### Siguientes pasos

- [Conceptos clave](/docs/introduction/key-concepts) - Comprende la pipeline de compilación y la arquitectura.
- [Resaltado de sintaxis](/docs/guides/syntax-highlighting) - Configura el resaltado de bloques de código en detalle.
- [compile()](/docs/packages/node/compile) - Referencia completa de la API para la función compile.
