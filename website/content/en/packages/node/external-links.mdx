---
title: "externalLinks()"
description: "Add rel and target attributes to external links in your Markdown output."
---

import { Example } from "~/components/Example"
import example from "./external-links-example.md?example"

```ts
import { externalLinks } from "@unifast/node";
```

## Signature

```ts
function externalLinks(options?: ExternalLinksPluginOptions): UnifastPlugin
```

## Parameters

### options?

Configuration for external link attributes

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| `rel` | `string` | `"nofollow noopener noreferrer"` | Value for the rel attribute on external links |
| `target` | `string` | — | Value for the target attribute (e.g., "\_blank") |

## Usage

```ts
import { compile, externalLinks } from "@unifast/node";

const result = compile(md, {
  plugins: [
    externalLinks({
      rel: "nofollow noopener noreferrer",
      target: "_blank",
    }),
  ],
});
```

## Examples

### External vs internal links

<Example data={example} />
