name: Coverage

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  rust-coverage:
    name: 🦀 Rust Coverage
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-rust
        with:
          components: llvm-tools-preview

      - name: 🔧 Install cargo-llvm-cov
        uses: taiki-e/install-action@6bd9352acd589686fbc4cff533c083c3366dd444 # cargo-llvm-cov

      - name: 📊 Generate coverage
        run: cargo llvm-cov --all-features --workspace --lcov --output-path rust-lcov.info

      - name: 📤 Upload to Codecov
        uses: codecov/codecov-action@e79a6962e0d4c0c17b229090214935d2e33f8354 # v6
        with:
          files: rust-lcov.info
          flags: rust
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

  ts-coverage:
    name: 🟢 TypeScript Coverage
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup-node

      - name: 🏗️ Build packages
        run: pnpm turbo build --filter='!@unifast/website'

      - name: 🧪 Run tests with coverage
        run: pnpm turbo test:coverage --filter='!@unifast/node' --filter='!@unifast/website'

      - name: 📊 Merge lcov files
        run: |
          mkdir -p coverage
          cat packages/*/coverage/lcov.info > coverage/ts-lcov.info 2>/dev/null || true

      - name: 📤 Upload to Codecov
        uses: codecov/codecov-action@e79a6962e0d4c0c17b229090214935d2e33f8354 # v6
        with:
          files: coverage/ts-lcov.info
          flags: typescript
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
